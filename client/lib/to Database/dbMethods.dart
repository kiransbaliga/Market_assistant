import 'dart:convert';
import 'dart:html';
import 'package:http/http.dart';

class DbMethods {
  void loginuser(String _email, String _password) async {
    // String basicAuth =
    //     'Basic' + base64Encode(utf8.encode('$_email:$_password'));
    // print(basicAuth);
    // String url;
    // Response r = await get(url, headers: {'authorization': basicAuth});
  }
}
